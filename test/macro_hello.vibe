; Canary for simple substitution macros (define-syntax / syntax-rules).
; (answer) expands to (llvm:const-int |i32| 42); linked main must exit 42.

(define-syntax answer
  (syntax-rules ()
    ((answer) (llvm:const-int |i32| 42))))

(llvm:define-function (main) |i32|
  (llvm:ret (answer)))
